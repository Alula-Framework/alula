/// Re-exported so `import AlulaPresence` brings the wire vocabulary —
/// `PresenceEntry`, `PresenceMeta`, `PresenceEvent`, `PresenceSync` — the
/// same way AlulaChannels re-exports its protocol target.
@_exported import AlulaPresenceProtocol

/// Re-exported so `import AlulaChannelsClient` brings the wire vocabulary —
/// `Envelope`, `JSONValue`, `ReservedEvent`, error reasons — without a
/// second import.
@_exported import AlulaChannelsProtocol

/// Re-exported so `import AlulaChannels` brings the wire vocabulary —
/// `Envelope`, `JSONValue`, `ReservedEvent`, the error reasons — without a
/// second import, the same way AlulaWeb re-exports the HTTP types.
@_exported import AlulaChannelsProtocol

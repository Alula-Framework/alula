Main.swift:18:23: error: [ALU-DI-1002] 2 modules provide `DataPool`, and it is asked for by type
    asked for by:
      OrderRepository.pool
    Composition never guesses between providers: picking one silently is how a service talks to the wrong database.
    help: say which one an unqualified @Inject means, in the module that lists them:
              static var defaultProviders: [any AlulaModule.Type] { [AnalyticsDataModule.self] }
          then name the other only where you want it:
              @Inject(from: PrimaryDataModule.self) var name: DataPool
    docs: https://github.com/Alula-Framework/alula/blob/main/Diagnostics/ALU-DI-1002.md
Main.swift:8:9: note: `AnalyticsDataModule.pool` provides `DataPool`
Main.swift:4:9: note: `PrimaryDataModule.pool` provides `DataPool`
